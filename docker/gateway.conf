server {
    listen 8080;
    server_name gateway;

    # Página simples de status; o container 'gateway' funciona como host central
    location / {
        default_type text/plain;
        return 200 "Gateway container running\n";
    }
}
